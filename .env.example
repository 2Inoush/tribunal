# Copier ce fichier en .env puis adapter les valeurs.
# .env n'est jamais versionne (voir .gitignore).

APP_URL=http://localhost:8000
APP_DEBUG=true

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=tribunal
DB_USER=root
DB_PASS=
